# 实时竞拍大师 - 核心功能测试用例文档

## 📋 目录
1. 单元测试运行指南
2. 分布式锁测试用例
3. 竞拍状态机FSM测试用例
4. 全场景集成测试清单
5. WebSocket实时同步测试场景

---

## 1. 单元测试运行指南

### 前置条件
```bash
# 1. 确保Redis已启动
docker-compose up -d redis

# 2. 进入Go后端目录
cd backend-go

# 3. 安装测试依赖
go get github.com/stretchr/testify
```

### 运行所有单元测试
```bash
go test -v ./pkg/lock/...
go test -v ./internal/service/...
```

---

## 2. 分布式锁测试用例

### 测试用例2.1：基础加锁解锁
| 项 | 详情 |
|----|------|
| 测试名称 | 基础加锁解锁流程验证 |
| 预期输入 | lockKey="test_basic_lock", 超时5s |
| 预期输出 | 1. Acquire成功返回token 2. Redis中key存在 3. Release成功后key消失 |
| 验证点 | 锁的正常生命周期 |

### 测试用例2.2：20并发安全累加
| 项 | 详情 |
|----|------|
| 测试名称 | 高并发场景下计数器原子性 |
| 预期输入 | 20个goroutine同时竞争锁，对counter累加1 |
| 预期输出 | 最终counter=20，无并发冲突导致的数据丢失 |
| 验证点 | 分布式锁完全保障互斥性 |

### 测试用例2.3：错误token不能释放锁
| 项 | 详情 |
|----|------|
| 测试名称 | 非持有者不能释放别人的锁 |
| 预期输入 | 锁A持有tokenA，尝试用错误tokenB释放 |
| 预期输出 | Release返回false，锁不会被误删 |
| 验证点 | Lua脚本原子校验持有者 |

---

## 3. 竞拍状态机FSM测试用例

### 测试用例3.1：状态机初始化
| 项 | 详情 |
|----|------|
| 测试名称 | 状态机初始化正确写入Redis |
| 预期输入 | goodsId=1001，起拍价0元，加价10元，时长300秒，自动延时15s |
| 预期输出 | 1. CurrentState=NOT_STARTED 2. CurrentPrice=0 3. RemainingMs=300000 |

### 测试用例3.2：完整正常竞拍流
| 步骤 | 事件 | 目标状态 |
|------|------|---------|
| 1 | 初始化 | NOT_STARTED |
| 2 | START | ONGOING |
| 3 | BID | ONGOING |
| 4 | 更新价格10元 | - |
| 5 | TIMEOUT | ENDED |
| 6 | 再次BID | 非法转移返回false |
| 验证点 | 已结束状态无法再接受出价，流程闭环 |

### 测试用例3.3：自动延时逻辑
| 项 | 详情 |
|----|------|
| 测试名称 | 最后15秒出价触发自动延时 |
| 前置状态 | ONGOING，剩余100秒 |
| 步骤1 | TickTimer 90000ms，剩余10秒 |
| 步骤2 | 发送BID事件 |
| 预期输出 | 状态变为DELAYING，剩余时间重置为15秒 |

### 测试用例3.4：异常竞拍取消
| 项 | 详情 |
|----|------|
| 测试名称 | 主播中途取消异常竞拍 |
| 前置状态 | ONGOING |
| 发送CANCEL事件 |
| 预期输出 | 状态直接变为CANCELLED，竞拍终止 |

---

## 4. 全场景集成测试清单

| 编号 | 集成测试场景 | 预期结果 |
|------|-------------|---------|
| 4.1 | 用户A、B、C同时进入同一个竞拍房间 | 三人WebSocket都成功加入auction:{goodsId}房间，互相同步接收消息 |
| 4.2 | 用户A首次出价 +10元 | 房间内所有人同时收到价格更新通知，当前价格同步为10 |
| 4.3 | 用户B紧随出价 +10元 | 所有人看到最新价格20，排行榜自动更新B到榜首 |
| 4.4 | 倒计时剩余10秒时用户C出价 | 自动延时15秒，所有人看到剩余时间从15s重新开始走 |
| 4.5 | 倒计时走完最后15秒 | 系统自动触发TIMEOUT，竞拍结束，成交价格停留在最后时刻 |
| 4.6 | WebSocket网络断开3秒后重连 | 自动指数退避重连成功，重连后自动拉取最新状态，无消息断层 |
| 4.7 | 200并发用户同时点击出价按钮 | 分布式锁串行化处理，无重复出价冲突，所有出价记录最终一致持久化 |
| 4.8 | 当前价达到封顶价 | 系统自动触发成交，竞拍立即结束，无需等待倒计时走完 |
| 4.9 | 未开始的竞拍收到BID事件 | FSM拒绝非法状态转移，出价被拦截 |
| 4.10 | 竞拍结束后新用户进入房间 | 可以查看最终成交结果，无法再出价 |

---

## 5. WebSocket实时同步测试场景

### 测试场景A：毫秒级同步一致性
**测试步骤**：
1. 启动3个不同浏览器标签页，同时进入同一个竞拍房间
2. 用户A在标签页1出价
3. 观察标签页2和标签页3的价格更新时间差
**预期结果**：所有人价格更新延迟 < 100ms，绝对没有出现A看到10元，B看到0元的状态不一致情况

### 测试场景B：心跳保活断线恢复
**测试步骤**：
1. 用户连接WebSocket正常进入房间
2. 通过浏览器DevTools → Network → Offline断网模拟30秒
3. 恢复网络连接
**预期结果**：自动重连成功，重连后状态同步完整，没有丢失之前的出价记录

---

## 6. 测试命令汇总
```bash
# 运行分布式锁全部测试
cd backend-go && go test -v ./pkg/lock/...

# 运行状态机全部测试
cd backend-go && go test -v ./internal/service/...

# 生成覆盖率报告
go test -coverprofile=cover.out ./...
go tool cover -html=cover.out -o coverage.html
```
